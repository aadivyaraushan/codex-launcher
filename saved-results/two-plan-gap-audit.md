# Two-plan gap audit
**Date:** 2026-08-03
**What for:** telling the loop orchestrator what is still open in `planning/consumer-app-implementation-plan.md`, verified against the repo, not against the plan's own checkboxes.
**Scope note:** the plan covers ~60 app rows across 5 waves plus an iOS track. This audit ranks the load-bearing gaps (Wave 0 exit clauses, the "this push" Pixel 9 table, and the Wave 1 stop-line items that block everything after them) rather than re-litigating every row. Wave 2/3/4 and the iOS shell are *correctly* not started — they are explicitly gated on later waves or a device that doesn't exist — and are not counted as gaps below.

## Re-checked 2026-08-03 (second pass)
Every row below was re-verified against what is on disk right now (`ls`/`find`/`grep -rn`/`go test` from `companion/`), not against the first pass's claims. Status is now one of CLOSED / OPEN / OWNER-BLOCKED.

**Counts:** 2 CLOSED, 3 OPEN, 8 OWNER-BLOCKED (13 rows total). The 8 OWNER-BLOCKED rows are also listed with concrete next actions in `saved-results/owner-action-pack.md`.

Rows whose status changed since the first pass: **2, 3, 5, 6, 7, 8, 9, 10, 11, 12** (row-by-row detail below). Row 1 and row 4 kept the same top-level label but the underlying evidence changed materially — see their rows. Row 13 changed label (PARTIAL → OPEN) with no material evidence change.

## Open items, ranked by what blocks the most

| # | Deliverable (plan section + line) | State | Evidence checked | What is actually missing |
|---|---|---|---|---|
| 1 | Messaging COMPLETE via Beeper Server (IG/Discord/Google Messages), plan lines 1217–1382, Pixel 9 table row 1 (line 26) | **OPEN** (unchanged label; evidence materially updated) | `grep -rli beeper . ../android` from `companion/` → no code hits (still true). But `saved-results/beeper-server-phone-linux-spike.md` now EXISTS (dated 2026-08-03, was missing at first pass): verdict **FAILED** — Beeper's only Linux build (Electron/AppImage) cannot execute in the phone's Linux userspace (`Could not find a PHDR: broken executable?`, exit 134/SIGABRT), so the Server never bound a port and step 6 (send) was never reached. | Zero implementation code, and now a completed spike showing the phone-side approach is a dead end for v1. Per the plan's own spike-fail exit table, Instagram DM/Google Messages/Discord stay at their pre-spike ceiling (not COMPLETE via `beeper_server_localhost`). This is a technical wall, not a one-click owner fix, so it stays OPEN rather than OWNER-BLOCKED — the next move (if any) is the Owner deciding whether to reopen Browserbase for Meta, which is a product call, not an unblock. |
| 2 | Spotify search + play COMPLETE (Web API, user OAuth), plan line 1502, Pixel table row 2 (line 27) | **OWNER-BLOCKED** (was OPEN) | `ls internal/capability/adapters/spotify/` → exists. `find internal/capability/runtime -iname "*spotify*"` → `spotify.go`, `spotify_test.go`. `grep -rn "NewSpotify" .` → used by `cmd/codex-launcher/spotify_proof.go` outside its own package. `go test ./...` → 1250 passed, 0 failed. `saved-results/wave1-spotify-complete.md` (2026-08-03): "Verdict: unverified... All code is built and unit-tested green... No Spotify refresh token exists anywhere in the repo." | The adapter, OAuth flow, search, and playback code are all built and green. The only remaining step is the Owner clicking through Spotify's one-time browser OAuth consent screen so a refresh token exists — an agent cannot fabricate that click or the resulting on-Pixel playback observation. |
| 3 | YouTube search + open COMPLETE, plan line 1503/1400, Pixel table row 3 (line 28) | **OWNER-BLOCKED** (was OPEN) | `ls internal/capability/adapters/youtube/` → exists. `grep -rn "NewYouTube" .` → used by `cmd/codex-launcher/youtube_proof.go`. `saved-results/wave1-youtube-maps-complete.md:11,37`: "search.list and videos.list both return live 403 PERMISSION_DENIED... a project/key-level API restriction, not a code bug." The "open COMPLETE" claim was withdrawn later on 2026-08-03 (see the Correction section at the end of `wave1-youtube-maps-complete.md`): `Adapter.Resolve` sends **both** `read` and `play` through the same `search.list` call, so the 403 takes both, and the video that played on the Pixel was started by a hand-typed `am start -a VIEW` intent that never touched the adapter or the capability flow. The manifest now reads `Ceiling: manifest.HandsOff` for the whole adapter. | Nothing has ever been carried to the end through this adapter, so there is no COMPLETE here to claim. Search is blocked by a Google Cloud Console-level restriction on `YOUTUBE_API_KEY` (the API is being refused at the project/key level) — that is a console setting only the Owner can change, not something fixable from inside this repo. |
| 4 | Maps places/directions + nav-intent COMPLETE, saved-places HAND-OFF, plan lines 1412–1414/1517–1518, Pixel table rows 4–6 (lines 29–31) | **OPEN** (unchanged label; 3 of 4 sub-rows now closed, and the file's own stated blocker for the 4th is stale) | `ls internal/capability/adapters/maps/` → `adapter.go`, `client.go` + tests. Places, nav-intent, and saved-places all driven live on Pixel per `saved-results/wave1-youtube-maps-complete.md` (screenshots + `dumpsys`/`topResumedActivity` evidence) — all three **COMPLETE**. Directions: that file claims a blocking architecture gap ("no caller anywhere... ever populates Fields"), but re-checking now: `grep -n "Fields" internal/capability/routing/stage1/route.go internal/capability/routing/stage2/resolver.go internal/capability/flow/service.go` shows Fields is wired stage1→stage2→`flow.Service.Prepare`→`adapter.Intent.Fields` (`flow/service.go:103` `Fields: decision.Fields`), and `internal/capability/adapters/maps/adapter.go:114-115` reads `in.Fields["origin"]`/`["destination"]`. `go test ./internal/capability/adapters/maps/... ./internal/capability/flow/... ./internal/capability/routing/...` → 116 passed. | Places, nav-intent, and saved-places are done with real Pixel evidence. Directions is code-complete and unit-tested (the "architecture gap" the evidence file describes no longer exists in the current code — that claim is itself stale), but has never been driven on-device, so there is no live proof the on-Pixel path actually produces a loaded route. That's an agent-doable task (run the drive), not an owner blocker, hence OPEN not OWNER-BLOCKED. |
| 5 | Pay + sensitive bookings/orders HAND-OFF minimum set (Uber, DoorDash, Venmo, + one booking app), Pixel table row 7 (line 32) | **OWNER-BLOCKED** (was PARTIAL) | `saved-results/wave1-handoff-pixel-evidence.md` (2026-08-03): Uber, DoorDash, and Venmo all force-stopped, relaunched, and screenshotted on the real Pixel — `topResumedActivity` confirms each official app's real launcher activity, copy verdict "honest" (no completion claims) for all three. `pm list packages | grep -iE 'opentable|airbnb'` → zero hits, re-checked 2026-08-03. | Uber/DoorDash/Venmo are done with real on-device evidence. The "+ one booking app" leg (OpenTable/Airbnb/Resy/Grubhub) is still unmet because none of those apps are installed on the shared Pixel — installing an app requires the Owner's own Play account, so this is now OWNER-BLOCKED rather than a code gap. |
| 6 | Instagram feed post/reel/story HAND-OFF, Pixel table row 8 (line 33) | **CLOSED** (was PARTIAL, "Pixel proof OPEN") | `saved-results/wave1-handoff-pixel-evidence.md`: Instagram reinstalled by the Owner 2026-08-03, driven twice from a cold force-stop via `monkey -c android.intent.category.LAUNCHER`; screenshots show the real Instagram home feed/stories carousel; copy verdict "honest — no 'posted' claim anywhere." `ls internal/capability/adapters/instagram/` → exists with tests. | Nothing outstanding — code, tests, and real on-device evidence all present. |
| 7 | Android notification-reply probe finished, per-app yes/no for Messages, WhatsApp, Instagram, Messenger, Signal — Wave 0 exit test, plan lines 1140–1144 | **OWNER-BLOCKED, 2/5** (was "OPEN, 2/5") | `saved-results/wave0-notification-reply-probe.md` "Re-check 2026-08-03" section: `adb shell pm list packages` against the Pixel shows Google Messages installed but has never posted a notification during any probe window; Messenger (`com.facebook.orca`/`.mlite`) and Signal (`org.thoughtcrime.securesms`) still absent. Score unchanged: WhatsApp and Instagram `CAN_REPLY`, the other three `NOT_MEASURED`. | Messages needs one real inbound SMS (protected broadcast, cannot be simulated via `adb`); Messenger and Signal need the Owner to install them under their own Play account. All three are one-time human acts. |
| 8 | Wave 0 exit test: all three proving adapters (RT-1 Notion, RT-4 Pixel reply, RT-6 Apple Notes) driven by hand with evidence, plan lines 1155–1160 | **OWNER-BLOCKED, 0/3** (was "OPEN, 0/3") | `saved-results/wave0-proving-adapters-hands-on.md`: RT-6 Apple Notes still `BLOCKED` on a macOS Automation permission click; RT-1 Notion still `BLOCKED` on the Owner's OAuth sign-in; RT-4 now shows the reply box **proven to exist** on two apps (real WhatsApp and Instagram DMs arrived and the listener swept them, `canned_only=0`), but firing a real reply is still `BLOCKED` — that needs the Owner's explicit authorization since it sends a real message to a real person. | RT-4's evidence narrowed (box proven, send still blocked on permission, not evidence) but the count stays 0/3 complete. All three remaining steps are one-time human acts (permission click, OAuth sign-in, authorize-a-real-send). |
| 9 | GATE — LEGAL: class B review booked, dated, plan lines ~1124/1166 area (consent/authorization gate section) | **OWNER-BLOCKED** (was OPEN) | `saved-results/wave0-gate-status.md:31-32` (still dated 2026-07-31, no newer file found) — "NOT CLEARED (not asked)." | Blocks all of Wave 3 plus the iMessage row in Wave 1. Requires emailing a lawyer with the three questions in that file — a human act, not code. |
| 10 | GATE — DISTRIBUTION (Apple): guideline read-through done, question sent to Apple, plan lines 1171-1174 | **OWNER-BLOCKED** (was PARTIAL) | `saved-results/wave0-apple-review-guideline-readthrough.md` (dated 2026-07-31, unchanged) confirms the read-through is done. `saved-results/wave0-gate-status.md:75` still says "I have not asked Apple" (that line itself predates the read-through file and is superseded by it, per the first pass's own note). | The read-through (agent-doable research) is complete. Sending the actual question to Apple Developer Relations is an outward-facing act the repo's agents will not take on the Owner's behalf — still outstanding. |
| 11 | GATE — CONSENT: exercised on a real (non-owner) class B connection, plan Testing/gates section | **OWNER-BLOCKED** (was OPEN) | `saved-results/wave0-gate-status.md:93` (unchanged, no newer consent-gate file found via `ls saved-results/ \| grep -i consent`) — framework built and unit-tested, but "what has not happened is a real class B connection being made, consented to, and revoked on real hardware." | **Re-diagnosed 2026-08-03: this is not owner-blocked, it is blocked on the Beeper spike, which failed.** `grep -rn ConsentB internal/capability/adapters/` returns nothing — every shipped adapter is class A, which needs no consent screen at all. The policy audit in `planning/consumer-app-implementation-plan.md` ("Authorization classes, after the completed policy audit") retired class B: consent can no longer stand in for vendor authorization, and the old class B rows are class H now. The one route that would bring class B back is the bridge exception, which the plan marks CURRENTLY DORMANT because the on-phone smoke failed on 2026-08-03. So the consent screen machinery in `internal/capability/consent/` is a deliberate standby, not dead code, and this drill cannot run until something revives class B. No owner act unblocks it. |
| 12 | GATE — CAPACITY: decisions for the 5 named apps (Spotify Developer Mode 5-user cap, Gmail 100-user CASA, YouTube quota, Discord DM scope, Splitwise commercial terms) | **OWNER-BLOCKED, 0/5** (was "OPEN, 0/5") | `saved-results/wave0-gate-status.md:99` (unchanged, no newer capacity-gate file found) — "The decisions themselves ... have not been made for any of the five." | Each needs an owner decision (apply for more quota / cap cohort / email vendor), recorded in the manifest's `capacity` field. None recorded. |
| 13 | Developer-app registration across all RT-1/RT-2 services, plan lines 1034–1041 | **OPEN** (was PARTIAL, label changed only — no material evidence change) | Google, Microsoft, Slack, Todoist, Notion, Spotify, Beeper, Google Maps/YouTube all have working OAuth/API flows or filled `.env` keys per `saved-results/v1-implement-readiness.md:23`, `wave1-google-oauth.md`, `wave1-microsoft-oauth.md`, `wave1-slack-oauth.md` — all unchanged since the first pass. | Registration confirmed only for the ~8 services actually being built now. The plan's ask covers every RT-1/RT-2 row across all waves (~40+ more apps in Wave 2–4); those consoles aren't registered yet, but most of those apps aren't due to be built yet either — not currently blocking, hence OPEN rather than OWNER-BLOCKED. |

## Items already done

| Deliverable | Evidence |
|---|---|
| DESIGN.md amended: 3 new state marks + notification-content purpose corrected | `DESIGN.md:100-115` (One tap left / Handed off / Unverified marks), `DESIGN.md:207-210` (notification-access line corrected from count to content, dated 2026-07-31) |
| Play content-access policy answered | `saved-results/wave0-play-notification-access-policy.md` — finding: Notification Listener is not on Play's declaration-form list; no demo video required, with named clauses |
| RT-1 reachability audit, yes/no/BD per row | `saved-results/rt1-reachability-audit.md` — ~18 data rows, each with a verdict |
| Capability contract + manifest schema + adapter registry | `companion/internal/capability/manifest/manifest.go`, `companion/internal/capability/registry/registry.go` (+ tests) |
| Router, stage1/stage2, disambiguation | `companion/internal/capability/routing/stage1/route.go`, `.../stage1/openai/client.go`, `.../stage2/resolver.go` |
| Contact graph: schema, 5 resolution rules, wipe path | `companion/internal/capability/routing/contacts/graph.go:53-62` (RuleAppNamed/RulePinned/RuleOnlyCandidate/RuleClearlyRecent/RuleAsk), `graph.go:137` (`Wipe`) |
| Routing eval set + harness | `companion/internal/capability/routing/eval/eval.go` + `testdata/routing-eval.json`; `go test ./companion/internal/capability/routing/...` → 44 passed per `wave0-exit-test-scorecard.md:127` |
| Authorization/hand-off gate with tests | `companion/internal/capability/handoff/outcome.go`, `manifest.go`, adapters all reference `hands_off` with tests (`handoff/outcome_test.go`, `manifest_test.go`) |
| Ceiling verification: tier-1/tier-2, wake heartbeat, auto-demotion, alerting | `companion/internal/capability/verification/verification.go:88` (Nightly cadence), `:260` (`Heartbeat`), `:289` (`shortfallAlertThreshold = 3`), `execution/telemetry_test.go` |
| Adapter kill switch via remote manifest, proven live | `saved-results/wave0-proving-adapters-hands-on.md:36-67` — `go run ./companion/cmd/proveadapter killswitch` verdict transcript |
| Outcome UI: 3 ceiling states on Android | `wave0-exit-test-scorecard.md:63-64` — `CapabilityOutcome.kt`, test asserts no two marks share a shape |
| iOS deferred correctly, no ceiling claimed | `wave0-exit-test-scorecard.md:130-152` — both shipping adapters corrected from `platform: both` to `platform: android` on 2026-07-31; grep for `PlatformBoth`/`PlatformIOS` confirms only test fixtures remain |
| RT-1/RT-4/RT-6 proven "end to end" as code paths (not yet hand-driven, see gap #8) | one contract, one registry, one router, adapters built for all three runtimes |
| Money gate: account named in writing, owner OK recorded | `saved-results/operator-agent-billing-account.md` — OpenAI, `ssdear@gmail.com`, personal, $500/mo approved ceiling, $50 practical stop, recorded 2026-07-31 (this supersedes the "UNMET" verdict in the earlier `wave0-exit-test-scorecard.md`, which predates this file) |
| Custody gate: token-store model written + rotation exercised | `saved-results/custody-gate-token-store-security-model.md`; `go run ./companion/cmd/proveadapter rotate` verdict in `wave0-exit-test-scorecard.md:206-212` |
| GATE — Google Play distribution: finding recorded (second branch of the clause) | Same file as row above; `wave0-exit-test-scorecard.md:234-242` |
| Wave 1 RT-2 stop-line (Todoist proof) | `saved-results/wave1-todoist-rt2-proof.md:14` — "**STOP LINE OPEN.**" (i.e., cleared) — real Pixel run created Todoist task `6h9w8XPM54Qj9fp8` on 2026-08-02 through Home Auto mode with preview confirmation |
| Eval set green including must-ask ambiguity cases | `wave0-exit-test-scorecard.md:125-128` |
| Spotify, YouTube, Maps adapter code (search/play, search/open, places/directions/nav-intent/saved-places) built and unit-tested | `companion/internal/capability/adapters/{spotify,youtube,maps}/`, `companion/internal/capability/runtime/{spotify,youtube}.go`; `go test ./...` → 1250 passed, 0 failed, re-run 2026-08-03 |
| Maps places, nav-intent, saved-places driven live on the real Pixel 9 with screenshots/`dumpsys` evidence | `saved-results/wave1-youtube-maps-complete.md` |
| ~~YouTube open driven live on the real Pixel 9~~ **withdrawn 2026-08-03** — the video was opened by a hand-typed `am start` intent, not by the adapter | `saved-results/wave1-youtube-maps-complete.md` (Correction section) |
| Uber/DoorDash/Venmo hand-off driven live on the real Pixel 9, copy verified honest | `saved-results/wave1-handoff-pixel-evidence.md` |
| Instagram feed hand-off driven live on the real Pixel 9 after Owner reinstalled the app, copy verified honest | `saved-results/wave1-handoff-pixel-evidence.md` |
| Fields (named-slot) wiring from stage1 through to adapter.Intent, closing what an earlier file described as an architecture gap | `companion/internal/capability/routing/stage1/route.go:127`, `.../stage2/resolver.go:101`, `companion/internal/capability/flow/service.go:103`; `go test ./internal/capability/adapters/maps/... ./internal/capability/flow/... ./internal/capability/routing/...` → 116 passed |

## Items that cannot be done autonomously

| Deliverable | Why (human step) |
|---|---|
| Android notification-reply probe: Messages, Messenger, Signal rows | Needs an inbound SMS to the owner's Pixel (protected broadcast, can't be faked via `adb`) and installing Messenger/Signal under the owner's own Play account |
| RT-6 Apple Notes proving run | macOS Automation permission is a one-click grant only the Mac's owner can make (controller/target pairing, per-app) |
| RT-1 Notion proving run | Notion's hosted MCP is OAuth-only and rejects bearer tokens — a person must sign in at a browser |
| RT-4 reply-on-Pixel proving run | Firing a real reply sends a message to a real person from the owner's account; only the owner can authorize that act (the reply box itself is now proven to exist on WhatsApp and Instagram) |
| Legal gate (class B review) | Needs a lawyer engaged and a dated review booked |
| Apple distribution gate — sending the question | Writing to Apple Developer Relations on the owner's behalf is explicitly something the repo's agents will not do (the guideline read-through itself is done) |
| Google Play submission / demo video (if later required) | Filing in Play Console requires the owner's developer account |
| Capacity gate decisions (Spotify/Gmail/YouTube/Discord/Splitwise) | Each is a business/product tradeoff (apply for quota increase, cap cohort, email a vendor) that only the owner can decide |
| Beeper first account link (2FA/QR) | Now moot for v1: the phone-side Beeper Server spike itself FAILED for technical reasons (broken Linux executable on-device), so this step isn't reachable yet regardless of account link |
| Spotify OAuth consent screen (no refresh token exists yet) | Needs the owner to approve one browser consent screen — code and tests are otherwise ready |
| YouTube Data API key restriction | `YOUTUBE_API_KEY` returns live 403 PERMISSION_DENIED on `search.list`/`videos.list` — needs the owner to fix the restriction in Google Cloud Console |
| Booking/order app for the hand-off minimum set | Neither OpenTable nor Airbnb (nor Resy/Grubhub) is installed on the shared Pixel; installing needs the owner's Play account |
| Money/consent/legal gate sign-offs generally | Each requires an explicit "OK, in writing" from the owner or a third party, which the plan's own standard treats as undelegable |
