# Handoff — OpenClaw phone-agent pivot (2026-08-12)

For the agent picking this up: you are mid-way through an 11-phase pivot of
Operator into an OpenClaw-based persistent agent that runs ON the phone
(Termux/proot Debian on the owner's Pixel 9), using this repo's Go adapters
as its tools. The governing plan is `planning/openclaw-phone-agent-plan.md`
(judge-PASSed; read it first). You are currently inside Phase 8.

## Where the code is

- Branch: `worktree-phase2-tool-bridge` (this was developed in a git worktree
  at `.claude/worktrees/phase2-tool-bridge`; the branch carries everything).
- Go tests run from the repo root: `go test -count=1 ./companion/...`
  (expected: 1474 passed in 110 packages).
- Android tests from `android/`: `./gradlew :app:testDebugUnitTest`
  (expected right now: 731 passed + the two new Unit 4 pins; one of them —
  ScenarioCatalogTest — is deliberately RED, see below).

## Phase status

| Phase | State |
|---|---|
| 1 OpenClaw alive in phone Debian | done |
| 2 Tool bridge (Go /v1/agent-tools) | done |
| 3-7 (plugin, gates, turn proxy, chat UI, triggers) | Mac side done + judged; ONLY on-device Pixel proofs remain |
| 8 Delete predetermined-function pipeline | units 1-3 committed; unit 4 red tests in place, implementation NOT started; unit 5 pending |
| 9 Boot persistence | not started |
| 10 Open-source packaging + README | not started |
| 11 End-to-end judged proof + push repo | not started |

## Phase 8 in detail (`planning/phase8-delete-pipeline-plan.md`)

Committed: `f54a4f4` (Unit 1, agent-side disconnect verb), `b59a978`
(Unit 2, Go deletion wave), `b4ebf34` (Unit 2.5, RunOnDevice — device work
answers the waiting agent call instead of journaling frames), `c4047ed`
(Unit 3, the five capability wire names pruned from schemas, Go validator,
Kotlin codec, fixtures, in lockstep; dead Kotlin tests deleted — one had
been hanging the suite forever in runBlocking).

Next work, in order:

1. **Unit 4 — Kotlin deletion wave.** The plan's Unit 4 section carries a
   verified cut-point map ("Survey deltas" bullet) down to line numbers.
   Two pinned tests exist and must NOT be edited:
   - `ScenarioCatalogTest` (testDebug) — RED now on purpose; goes green
     when the six CAPABILITY_* scenario entries are deleted.
   - `a home prompt starts a task even when the companion advertises
     capability actions` in `LauncherSessionViewModelTest` — green now via
     an exception fallback; must stay green when the capability branch in
     `submitHomePrompt` is deleted outright.
   Key traps: `capability/reply/`, `capability/notifications/`, and
   `capability/handoff/youtube/` are UNRELATED features that survive;
   `StateMark`/`StateMarkView` are shared design primitives to relocate
   (task/summary), not delete; `LocalStateWiperInstrumentedTest` calls
   `fromStores` with 12 POSITIONAL args — edit in lockstep with
   `LocalStateWiper`; verify `:app:compileDebugAndroidTestKotlin` too,
   instrumented tests must compile even though they don't run on the Mac.
2. **Unit 5 — orphan sweep + full matrix + judge.** Grep list and allowed
   negative-fixture exceptions are written in the plan's Unit 5 section.
   Then a FRESH judge agent derives its own checklist from the master
   plan's Phase 8 bullet and attacks the whole Phase 8 diff
   (`d94fef4..HEAD`). Only after its PASS is Phase 8 done.

## Standing owner decisions (do not relitigate)

- Real-user verification is adb screen-driving ONLY: taps/swipes/typed
  text/screenshots; banned as proof: adb intents, test hooks, curl standing
  in for UI. Evidence = timestamped screenshots in `saved-results/`.
- Chat-first UI; README modeled on hindsight/pgGraph; final deliverable is
  the pushed repo `github.com/aadivyaraushan/codex-launcher`.
- OAuth/auth on device: use the same credentials already on the owner's
  phone. Approved paid account for OpenClaw API usage: ssdear@gmail.com
  (recorded in `saved-results/`). NEVER set up or run anything that costs
  money without stating the account and getting explicit OK first.
- No secrets in committed files or logs — token PATHS in config, never
  values; never log message text or people's names (ids only).

## Tooling traps on the owner's Mac (learned the hard way)

- Backgrounded `./gradlew` runs get killed silently by the shell harness.
  Run gradle detached: `sh -c 'nohup ./gradlew <task> > /tmp/x.log 2>&1 &'`
  and poll the log for `BUILD SUCCESSFUL|BUILD FAILED`.
- Never trust an exit code that passed through a pipe (`| tail`) — read the
  log. Gradle's cache and `rtk` can both dress a failure as success; a
  "BUILD SUCCESSFUL in 8s" is only believable after checking the test task
  executed and counting result XMLs in
  `android/app/build/test-results/testDebugUnitTest/`.
- A hung suite is usually a deleted-pipeline test blocking in `runBlocking`
  on a frame the codec now rejects — jstack the Gradle Test Executor to
  find it; don't wait it out.
- A locked Pixel fails the connected suite — check the keyguard before
  blaming a test.
